{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug tests",
            "cargo": {
                "args": [
                    "test",
                ],
            },
            "cwd": "${workspaceFolder}"
        }
    ]
}
